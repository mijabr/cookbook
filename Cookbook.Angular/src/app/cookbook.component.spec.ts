/*import {
  beforeEachProviders,
  describe,
  expect,
  it,
  inject
} from '@angular/core/testing';
import { CookbookAppComponent } from '../app/cookbook.component';

beforeEachProviders(() => [CookbookAppComponent]);

describe('App: Cookbook', () => {
  it('should create the app',
      inject([CookbookAppComponent], (app: CookbookAppComponent) => {
    expect(app).toBeTruthy();
  }));

  //it('should have as title \'cookbook works!\'',
  //    inject([CookbookAppComponent], (app: CookbookAppComponent) => {
  //  expect(app.title).toEqual('cookbook works!');
  //}));
});*/
